package org.oiue.service.action.http.resource;

public class A {
	public final String getName() {
		// int initialCapacity = 0;
		// float loadFactor = 0.75f;
		// Map m = new HashMap<>(initialCapacity, loadFactor);
		// List list = new ArrayList<>(initialCapacity)
		return "";
	}
}
